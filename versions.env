# Tool versions for source-compiled packages
# Only needed for install-*.sh advanced scripts (building from source)
# Standard packages are installed via apt and always get latest

GIT_SOURCE_VERSION="2.53.0"
CMAKE_SOURCE_VERSION="4.2.3"
